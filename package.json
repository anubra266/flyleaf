{
  "name": "flyleaf",
  "version": "0.2.0",
  "description": "Safari-style reader mode for serialized fiction — clean chapters, arrow-key navigation, per-site memory.",
  "private": true,
  "license": "MIT",
  "author": "anubra266",
  "homepage": "https://github.com/anubra266/flyleaf",
  "repository": {
    "type": "git",
    "url": "https://github.com/anubra266/flyleaf.git"
  },
  "scripts": {
    "build": "find . -name .DS_Store -delete && rm -f dist/flyleaf-*.zip && mkdir -p dist && zip -r -X \"dist/flyleaf-$(node -p \"require('./manifest.json').version\").zip\" manifest.json src vendor icons -x '*.DS_Store'",
    "bump": "node scripts/bump.mjs patch",
    "bump:minor": "node scripts/bump.mjs minor",
    "bump:major": "node scripts/bump.mjs major",
    "release": "npm run bump && npm run build"
  }
}
